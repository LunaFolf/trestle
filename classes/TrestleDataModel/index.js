/**
 * TrestleDataModel is a base class, to be extended, that can be used by TrestleDatabase class.
 * @class TrestleDataModel
 */

class TrestleDataModel {
  _modelName = undefined // Model Name

  constructor (sequelize, { DataTypes, Model }) {
  }
}